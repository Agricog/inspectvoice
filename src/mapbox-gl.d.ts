// Stub for transitive type reference from mapbox-gl
declare module '@mapbox/point-geometry' {
  class Point {
    x: number;
    y: number;
    constructor(x: number, y: number);
  }
  export default Point;
}
