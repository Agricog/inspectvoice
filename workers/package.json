{
  "name": "inspectvoice-api",
  "version": "1.0.0",
  "private": true,
  "description": "InspectVoice Cloudflare Worker API — voice-first playground inspection platform",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "fflate": "^0.8.2",
    "pdf-lib": "^1.17.1",
    "xlsx": "^0.18.5"
}
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.68.0"
  }
}
